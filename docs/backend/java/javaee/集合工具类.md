---
date: 2026-07-01 10:25:39
---

# 集合工具类

## Collections 工具类

Collections 工具类主要用于操作 Collection 及其子类和 Map。



### 排序/反转

|   方法    | 描述         |
| :-------: | ------------ |
|  sort()   | 集合排序     |
| reverse() | 反转集合     |
| shuffle() | 随机打乱集合 |

```java {5,9,13,17,21,25}
public static void main(String[] args) {
  List<Integer> list = Arrays.asList(3, 1, 4, 2, 5);

  // 排序
  Collections.sort(list);
  System.out.println("排序后：" + list);

  // 二分查找（使用前必须对集合先排序）
  int index = Collections.binarySearch(list, 2);
  System.out.println("元素2的索引" + index);

  // 反转
  Collections.reverse(list);
  System.out.println("反转后：" + list);

  // 获取最大值和最小值
  System.out.println("最大值：" + Collections.max(list));
  System.out.println("最小值：" + Collections.min(list));

  // 替换元素
  Collections.replaceAll(list, 1, 10);
  System.out.println("替换1为10后：" + list);

  // 随机打乱
  Collections.shuffle(list);
  System.out.println("随机打乱后：" + list);
}
```



### 查找/替换

|      方法      | 描述                       |
| :------------: | -------------------------- |
| binarySearch() | 二分查找指定元素，返回索引 |
| max() / min()  | 查找集合中最大值/最小值    |
|                |                            |







## Arrays 工具类

Arrays 工具类主要用来操作数组（如排序、搜索等）。
