---
date: 2026-08-27 20:44:12
---

# 依赖管理

在 Java 开发中，依赖（Dependency）就是你的项目需要用到的第三方代码库，而 Maven 的依赖管理，就是一个自动帮你下载、管理和更新这些代码库的“大管家”。



## 核心配置

`pom.xml` 是 Maven 项目的核心配置文件，该文件中有一些关键参数需要了解。

::: code-group

```xml [pom.xml] {34,42}
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <!-- 当前 pom.xml 文件遵循的 Maven 模型版本 -->
  <modelVersion>4.0.0</modelVersion>

  <!-- 当前项目的 G，定义所属的组织或公司，通常使用公司域名倒写 -->
  <groupId>org.example</groupId>
  <!-- 当前项目的 A，定义具体项目或模块的名称 -->
  <artifactId>demo1</artifactId>
  <!-- 当前项目的 V，定义项目的版本号 -->
  <version>1.0-SNAPSHOT</version>
  <!-- 当前项目的 P，定义项目最终要打包为什么格式 -->
  <packaging>jar</packaging>

  <!-- 项目的展示名称 -->
  <name>demo1</name>
  <!-- 项目的官方主页地址，如果代码开源，这里填写 Github 地址 -->
  <url>http://maven.apache.org</url>

  <!-- 定义项目的全局属性 -->
  <properties>
    <!-- 指定源代码的 Java 语言版本 -->
    <maven.compiler.source>21</maven.compiler.source>
    <!-- 指定编译后生成字节码文件的 JVM 版本 -->
    <maven.compiler.target>21</maven.compiler.target>

    <!-- 上面两个标签的合体 -->
    <!-- <maven.compiler.release>21</maven.compiler.release> -->

    <!-- 指定使用 UTF-8 编码来读取和编译源代码 -->
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- 提取 lombok 的依赖 -->
    <lombok.version>1.18.46</lombok.version>
  </properties>

  <dependencies>
    <!-- 引入 lombok 依赖 -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <version>${lombok.version}</version> <!-- 使用 ${} 的方式插入 -->
      <scope>compile</scope>
    </dependency>
  </dependencies>
</project>

```

:::



## 依赖作用域

依赖作用域用于控制依赖库在项目的哪个阶段起作用。常见的作用域有：

|  作用域  | 描述                                                         |
| :------: | ------------------------------------------------------------ |
| compile  | 默认。编译、测试、运行阶段都需要该库                         |
|   test   | 只在测试阶段有效，打包发布时不会包含它                       |
| provided | 编译和测试时需要，但真正在服务器上运行时，有服务器（如 Tomcat）来提供，不需要打包进去 |

```xml {5,12,19}
<dependency>
  <groupId>org.projectlombok</groupId>
  <artifactId>lombok</artifactId>
  <version>1.18.46</version>
  <scope>compile</scope>
</dependency>

<dependency>
  <groupId>org.junit.jupiter</groupId>
  <artifactId>junit-jupiter-api</artifactId>
  <version>6.1.3</version>
  <scope>test</scope>
</dependency>

<dependency>
  <groupId>javax.servlet</groupId>
  <artifactId>javax.servlet-api</artifactId>
  <version>4.0.1</version>
  <scope>provided</scope>
</dependency>
```



## 依赖传递

依赖传递分为两种：

- **直接依赖**：在当前项目中通过依赖配置建立的依赖关系；
- **间接依赖**：当前项目依赖了其他项目，其他项目中的依赖被当前项目所依赖；

> [!NOTE] 依赖冲突的优先原则
>
> - **路径优先**：当项目中间接依赖了相同的资源，层级越深，优先级越低，层级越浅，优先级越高；
> - **声明优先**：当项目中间接依赖了相同的资源，配置顺序靠前的覆盖配置顺序靠后的；
> - **特殊优先**：当项目中直接依赖了相同资源的不同版本，后面配置的覆盖先配置的；

::: code-group

```xml [声明优先]
<dependencies>
  <dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <version>1.18.46</version>
  </dependency>

  <!-- 上面的 lombok 依赖比 demo2 的配置更靠前，因此它覆盖了 demo2 中的依赖 -->
  <dependency>
    <groupId>org.example</groupId>
    <artifactId>demo2</artifactId>
    <version>1.1.0</version>
  </dependency>
</dependencies>
```

```xml [特殊优先]
<dependencies>
  <dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <version>1.18.44</version>
  </dependency>

  <!-- 上面的 lombok 依赖比 demo2 的配置更靠前，因此它覆盖了 demo2 中的依赖 -->
  <dependency>
    <groupId>org.example</groupId>
    <artifactId>demo2</artifactId>
    <version>1.1.0</version>
  </dependency>

  <!-- 同一个项目出现相同的依赖，后面的依赖覆盖前面的 -->
  <dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <version>1.18.46</version>
  </dependency>
</dependencies>
```

:::



## 可选依赖

可选依赖是指被依赖的项目通过配置 `<optional>` 选项，**对外隐藏当前项目的依赖项**。

```xml {7}
<dependencies>
  <dependency>
    <groupId>org.projectlombok</groupId>
    <artifactId>lombok</artifactId>
    <version>1.18.46</version>
    <!-- 隐藏此依赖，外界无法获取，默认值为 false -->
    <optional>true</optional>
  </dependency>
</dependencies>
```



## 排除依赖

排除依赖是指引用依赖的项目，**主动排除不需要的资源**。

```xml {7-12}
<dependencies>
  <dependency>
    <groupId>org.example</groupId>
    <artifactId>demo2</artifactId>
    <version>1.1.0</version>
    <!-- 主动排除 lombok 依赖，不使用子项目的依赖 -->
    <exclusions>
      <exclusion>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
      </exclusion>
    </exclusions>
  </dependency>
</dependencies>
```

